**1.代码布局**

1）.当 `if` 语句的条件部分足够长，需要写成多行时，值得注意的是，两个字符的关键字（即 `if`），加上一个空格，再加上一个开括号，为多行条件的后续行创建了一个自然的 4 空格缩进。如：

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-12-19-image.png)

2）.多行结构上的闭合大括号/方括号/圆括号可以与列表最后一行的第一个非空白字符对齐或者与开始多行结构的行的第一个字符对齐，如：![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-14-08-image.png)

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-14-20-image.png)

3）.python禁止混合使用**制表符和空格**进行缩进，所有行限制为**79**个字符，对于结构限制较少的长文本块（文档字符串或注释），行长应限制为**72** 个字符

4）.二元运算符在**换行之后**，如：

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-19-07-image.png)

5）.导入总是放在**文件的顶部**，紧随**模块注释和文档字符串**之后，以及**模块全局变量和常量**之前，并按以下顺序分组：

1. 标准库导入。
2. 相关的第三方导入。
3. 本地应用程序/库特定的导入。

您应该在每组导入之间留一个空行。

导入有绝对导入（**推荐这种**）和相对导入，如下：
![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-25-23-image.png)

（第一行是绝对导入，第二、三行是相对导入）

6）模块级“双下划线”（即具有两个前导和两个尾随下划线的名称），如 `__all__`、`__author__`、`__version__` 等，应放在模块文档字符串之后，但 *除* `from __future__` 导入之外的任何导入语句之前。Python 规定 future-imports 必须出现在模块中，位于任何其他代码之前，除了文档字符串，如：

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-27-58-image.png)

7）.当 `=` 符号用于指示关键字参数时，或用于指示 *未注解* 函数参数的默认值时，请勿在 `=` 符号周围使用空格

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-31-19-image.png)

但是，当将参数注解与默认值结合使用时，请在 `=` 符号周围使用空格

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-31-47-image.png)

8）创建单个元素的元组是，结尾的逗号是必须的

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-41-19-image.png)

每一行语句结尾可以有**尾随逗号**，但此时将闭括号/方括号/大括号放在下一行，尾随逗号和括号在同一行是无意义的

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-15-43-31-image.png)

9）**块注释**的每一行都以 `#` 和一个空格开头（除非它是注释内的缩进文本），块注释内的段落用包含**单个 `#`** 的行分隔，**行内注释**是与语句在同一行上的注释。行内注释应与语句**至少相隔两个空格**。它们应以 “# 和一个空格” 开头。

10）**命名约定**：切勿将字符 'l'（小写字母 el）、'O'（大写字母 oh）或 'I'（大写字母 eye）用作单字符变量名。

在某些字体中，这些字符与数字一和零无法区分。当想要使用 'l' 时，请改用 'L'。

11）.常量通常在模块级别定义，并以全大写字母书写，单词之间用下划线分隔。示例包括 `MAX_OVERFLOW` 和 `TOTAL`

**2.简单数据类型**

1）.int最多可以有4300位

2）整形进制

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-16-21-13-image.png)

3）.进制转换函数

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-16-23-10-image.png)

4）.int类型首次开辟为**28字节**，之后随着数字增大而增大，每次增量为**4字节**，可以**用 sys.getsizeof()函数** 获取数字字节大小

5）.float类型：**.123**=**0.123**，**3.14e2**=**3.14*10^2**

6).浮点数解决**精度损失**的办法（以9.9/3为例）：

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-18-23-28-image.png)**3.复合数据类型**

1）.当字符串里的内容较复杂时，可以用三个**双引号**或三个**单引号**

2）.python没有**字符**类型

3）。字符串驻留机制：当满足以下条件时，如果多个指向同一个内容的字符串，他们在内存的地址**相同**

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-20-27-11-image.png)

（如果不满足以上条件，需要在python原生环境**cmd**才能看到差别）

4）.字符串只能和字符串类型相加，转换函数有**int(),float(),str()**,字符串指定的内容可以转换成int和float

5）.列表list[a: b :c]中,a表示**起始位置**，b表示**终止位置**，c表示**步长**，起始位置会包含在切片中，终止位置不会包含在切片中

6）.关于列表的一些操作

![](C:\Users\ZhuanZ\AppData\Roaming\marktext\images\2026-02-04-20-52-21-image.png)


